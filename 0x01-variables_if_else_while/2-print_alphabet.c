#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	for(s = "a"; s <= "z", x++)
	{
		putchar(s);
		putchar("\n");
	}

	return (0);
}
