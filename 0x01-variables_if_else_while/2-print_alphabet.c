#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	for(int s = "A"; s <= "Z", x++)
	{
		int low_alph = to_lower(s);
		putchar(low_alph);
	}

	return (0);
}
