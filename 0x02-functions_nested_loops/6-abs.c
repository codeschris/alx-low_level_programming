#include "main.h"

/**
 * _abs - Check Description
 *
 * Description: gives absolute value of given integer
 *
 * Return: Nothing
 */

int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (i * -1);
}
