#include "main.h"

/**
 * Function islower - Check Description
 *
 * Description: The function checks for lowercase letters
 *
 * Return: 0 or 1
 */

int _islower(int c)
{
	char a;
	
	for (a = 'a'; a <= 'z'; a++)
		return (0);
	for (a = 'A'; a <= 'Z'; a++)
		return (1);
}
