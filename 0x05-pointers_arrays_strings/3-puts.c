#include "main.h"

/**
 * _puts - function prints a string, followed by newline
 * to stdout
 *
 * @str: input string
 *
 * Return: nothing
 */

void _puts(char *str)
{
	char x;

	str = &x;
	fputs(x, stdout);
}
